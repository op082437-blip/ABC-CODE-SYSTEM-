ABC–XYZ CODE APP

Files:
index.html = app screen
style.css = appearance
app.js = converter logic
manifest.json = install/app information
service-worker.js = offline caching

To use it as an installable app, upload these files to a HTTPS static website.
Then open the site on your phone/desktop and choose Add to Home Screen or Install App.

The conversion happens locally in the browser.